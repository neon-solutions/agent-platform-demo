node_modules
.next
out
dist
build

# env
.env
.env.local
.env*.local
!.env.example

# neon
.neon

# vercel
.vercel

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Vendored agent skills (local tooling). Only the agent-platforms skill —
# the one this platform is built around — is committed; the rest stay local.
.agents/skills/*
!.agents/skills/neon-postgres-agent-platforms
